# Git LFS configuration for Squiggy
# Track POD5 files with Git LFS to avoid bloating the repository

# POD5 test data files
tests/data/*.pod5 filter=lfs diff=lfs merge=lfs -text

# Optional: Track other large binary files if needed
*.fast5 filter=lfs diff=lfs merge=lfs -text
