{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "${workspaceFolder}/test-refactor-plotting-architecture-25f5c6c",
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/build/**/*.js"
      ],
      "preLaunchTask": "prepare-extension",
      "env": {
        "PYTHONPATH": "${workspaceFolder}:${env:PYTHONPATH}"
      }
    }
  ]
}
